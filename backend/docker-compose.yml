version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: mongodb+srv://change20U:YkliB3wirfhVJFVC@change20dev.uj6bor5.mongodb.net/change20_db
      BASE_URL: https://devwebinar.change20.no
      DEBUG: "True"
      # OpenAI 429 bypass - set true on live when quota exhausted
      MOCK_OPENAI_MODE: ${MOCK_OPENAI_MODE:-true}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
    volumes:
      - .:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
